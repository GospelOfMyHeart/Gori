import { PositionCategoryModel } from './position-category';

describe('PositionCategory', () => {
  it('should create an instance', () => {
    expect(new PositionCategoryModel()).toBeTruthy();
  });
});
