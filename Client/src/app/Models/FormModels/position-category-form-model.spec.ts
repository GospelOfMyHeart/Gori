import { PositionCategoryFormModel } from './position-category-form-model';

describe('PositionCategoryFormModel', () => {
  it('should create an instance', () => {
    expect(new PositionCategoryFormModel()).toBeTruthy();
  });
});
